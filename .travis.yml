language: python

matrix:
  include:
    - python: 3.7
      dist: xenial
      sudo: true
    - python: 3.6
      dist: xenial
      sudo: true

stages:
  - test
  - name: deploy
    if: branch = master

jobs:
  include:
    - stage: test
      install:
        - pip install -r requirements.txt
        - pip install -r requirements-e.txt
      script:
        - flake8 .
        - mypy --ignore-missing-imports .
    - stage: deploy
      script:
        - echo deploy
      python: 3.7
